# Simple Memory

Experimental plugin used to prove out the [assistant plugin system](../README.md) end-to-end before the real memory logic moves into the runtime harness directly. Every hook a memory system would need is wired with the thinnest possible implementation behind it; the value of this plugin is the surface, not the behavior.
