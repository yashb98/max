# Experimental plugins

Scratch space for plugin scaffolds that prove out the assistant plugin system before the real implementations land in the [`/assistant`](../../assistant) module. Each subdirectory is a self-contained plugin package; nothing here is shipped with the runtime.
