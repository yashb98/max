/**
 * Stub module — superseded by tts.test.ts.
 *
 * The `assistant tts synthesize` command has been migrated to IPC transport.
 * All tests now live in tts.test.ts, which follows the memory-v2 pattern and
 * validates the cliIpcCall dispatch path.
 */
